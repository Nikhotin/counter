extends layout

.header
  block header
    a.button(href="/counter/:id") Your counter
    a.button(href="/counter") All counter
    a.button(href="/profile/:id") Profile
.sidebar-left
.main
  block main
    h1= message
    h2= error.status
    pre #{error.stack}     
.sidebar-right
.footer
  block footer
    form(action='/logout')
      button(type='submit') Log out
